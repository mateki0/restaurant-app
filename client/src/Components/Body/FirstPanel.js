import React from "react";
const FirstPanel = () => (
  <div data-testid="first-panel-div" className="name-container">
    <div className="name">
      <h1>My Test Restaurant</h1>
      <span>
        I should write some description here, but this page is only for learning
        resons, so it doesn't matter
      </span>
    </div>
  </div>
);
export default FirstPanel;
