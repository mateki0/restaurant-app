import styled from 'styled-components';

const FooterInnerContainer = styled.div`
  width: 80vw;
  display: flex;
  justify-content: space-between;
  margin: 0 auto;
`;
export default FooterInnerContainer;
